local config = {}

config.controls = {}

config.controls.game = {
  jump = "up",
  right = "right",
  left = "left",
}

config.controls.mapCreator = {
  blockMenu = "m",
  scrollRight = "right",
  scrollLeft = "left"
}

return config
